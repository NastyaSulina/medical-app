import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        width: '100%',
        paddingBottom: 12,
    },
    content: {
        maxWidth: '90%',
        minHeight: '100%',
    },
});

export default styles;
