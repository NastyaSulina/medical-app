import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        flexGrow: 1,
        alignItems: 'center',
    },
    content: {
        maxWidth: '90%',
    },
});

export default styles;
