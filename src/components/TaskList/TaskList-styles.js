import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    task: {
        marginTop: 12,
    },
    separator: {
        borderTopWidth: 0.5,
        borderColor: '#9FAABB',
        marginTop: 16,
    }
});

export default styles;
