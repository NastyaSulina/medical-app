import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    input: {
        borderWidth: 0.5,
        borderColor: '#D6DEED',
        borderRadius: 12,
        padding: 16,
        width: '100%',
        marginBottom: 16
    },
});

export default styles;
