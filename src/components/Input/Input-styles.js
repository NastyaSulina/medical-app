import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    input: {
        border: '0.5px solid #D6DEED',
        borderRadius: 12,
        padding: 16,
        width: '100%',
    },
});

export default styles;
