import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    slider: {
        width: '100%',
    },
    thumb: {
        width: 19,
        height: 19,
        left: -148,
        top: 14,
    },
});

export default styles;
